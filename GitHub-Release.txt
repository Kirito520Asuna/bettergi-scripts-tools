### 版本变更详情

- 重构缓存目录初始化逻辑，将缓存目录创建从 `CacheRunner` 迁移至 `BgiToolsApplication` 的 `@PostConstruct` 方法，确保在应用启动时尽早完成目录初始化
- 移除 `CacheRunner` 的 Spring 组件注解和 `ApplicationListener` 实现，降低不必要的耦合
- 改用 `SpringUtil` 获取 `Environment` 读取配置属性，统一配置获取方式
- 将缓存目录创建方法简化为静态工具方法，提升复用性与可测试性
- 新增应用关闭时的资源清理方法框架，为后续优雅关闭做准备